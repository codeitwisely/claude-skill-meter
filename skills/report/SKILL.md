---
description: Show the current session's tool-call breakdown and estimated cost. Use when the user asks "how much did that cost?", "show session cost", "cost report", "what's my token usage this session?", or "how expensive was that skill?".
---

Run `csm-report` via Bash and display its output verbatim.

If the command is not found, inform the user that the plugin's `bin/` directory must be on PATH — this is handled automatically when the plugin is loaded via `--plugin-dir` or installed from the marketplace.
