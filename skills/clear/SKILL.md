---
description: Archive the current session log and start a fresh tracking session. Use when the user says "reset cost tracker", "clear session", "start fresh", or "new session meter".
---

Run `csm-report --clear` via Bash and display its output.
